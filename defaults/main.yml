---
# defaults file for ansible-role-postfix

# Set postfix state when configuration changes are made. Recommended values:
# `restarted` or `reloaded`
postfix_restart_state: restarted

postfix_configuration_items: []
#  - regexp: "^relayhost = "
#    line: "relayhost = [smtp.mailgun.org]:587"

postfix_configuration_files: []
#  - src: "{{ inventory_dir }}/templates/postfix/main.cf.j2"
#    dest: "main.cf"
#    force: no